/* write a function 'multiply' that can take one, or two arguments.
 * If it takes one argument it should return a function
 * that multiplies any future argument by the first argument.
 *
 * eg. multiply(10, 10) == 100
 *     const multBy10 = multiply(10)
 *     multBy10(5) == 50
 */

function multiply(a, b) {
    console.log(a, b);
}

module.exports = multiply;
